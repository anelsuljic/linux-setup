- `config/hypr` contains the configuration files (well structures with subdirectories when needed) that should be placed at `~/.config/hypr`.

- `setup-sddm.sh` should be executed after installing sddm through `System -> Display Manager`. `sddm` folder contains auxiliary files for the script.
